/** Last known mouse position in React Flow canvas (flow) coordinates.
 *  Updated by MouseTracker inside InfraCanvas. Read by graphStore when spawning nodes. */
export const canvasMousePos = { x: 300, y: 200 };
